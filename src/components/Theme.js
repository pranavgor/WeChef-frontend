import { styled, createTheme, ThemeProvider } from '@mui/material/styles';
export const theme = createTheme({
    palette: {
    mode: 'dark',
    primary:{
        light:"#f3e5f5",
        main:"#ce93d8",
        dark:"#ab47bc"
    }
  },
});
